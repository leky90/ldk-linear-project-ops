# Claude Code instructions

Read and follow `AGENTS.md`. This repository is the reusable source and packaging
workspace for `ldk-linear-project-ops`, not a bound Linear project.

Keep plugin behavior generic, fixtures synthetic, and runtime project configuration
outside Git. Validate code, skills, schemas, and the plugin package before handoff.
