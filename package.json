{
  "name": "ldk-local-claim-lock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "ldk-claim-lock": "./claim-lock/cli.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node --check claim-lock/*.mjs test/*.test.mjs",
    "check": "npm run lint && npm test"
  }
}
