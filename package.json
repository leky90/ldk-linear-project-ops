{
  "name": "ldk-agent-workflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "ldk-agent": "./src/cli.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node --check src/*.mjs test/*.test.mjs",
    "check": "npm run lint && npm test"
  }
}
