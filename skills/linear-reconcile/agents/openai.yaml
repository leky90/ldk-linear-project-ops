interface:
  display_name: "Reconcile Linear Work"
  short_description: "Repair lifecycle and legacy inconsistencies"
  default_prompt: "Use $linear-reconcile to repair inconsistent Project lifecycle status, issue handoffs, or legacy workflow data."
